# -----------------------------------------------------------------------------
# Local development (Docker Compose)
# -----------------------------------------------------------------------------
POSTGRES_DB=dwc
POSTGRES_USER=dwc
POSTGRES_PASSWORD=dwc

# Used by Prisma/Next.js. In Docker Compose, host MUST be `db`.
DATABASE_URL=postgresql://dwc:dwc@db:5432/dwc?schema=public

# Prisma: force native engine for local dev (avoids "engine type client requires adapter/accelerateUrl")
PRISMA_CLIENT_ENGINE_TYPE=binary

# Optional pgAdmin (only used if you start `pgadmin` profile)
PGADMIN_DEFAULT_EMAIL=admin@local.test
PGADMIN_DEFAULT_PASSWORD=admin

# -----------------------------------------------------------------------------
# App/Auth (planned; placeholders)
# -----------------------------------------------------------------------------
# Auth.js / NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-me-with-a-long-random-secret
AUTH_SECRET=replace-me-with-a-long-random-secret

# -----------------------------------------------------------------------------
# Site/Adsense (placeholders)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_ADSENSE_CLIENT=
NEXT_PUBLIC_ADSENSE_SLOT_TOP=
NEXT_PUBLIC_ADSENSE_SLOT_MIDDLE=
NEXT_PUBLIC_ADSENSE_SLOT_BOTTOM=
NEXT_PUBLIC_ADSENSE_SLOT_POST=
NEXT_PUBLIC_ADSENSE_SLOT_HOME=
NEXT_PUBLIC_ADSENSE_SLOT_SHOP=
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Apple OAuth
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Payments (planned; placeholders)
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Shipping (planned; placeholders)
# -----------------------------------------------------------------------------
SHIPPO_API_TOKEN=

# -----------------------------------------------------------------------------
# Email (planned; placeholders)
# -----------------------------------------------------------------------------
RESEND_API_KEY=
RESEND_FROM=

