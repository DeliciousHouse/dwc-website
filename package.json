{
  "name": "dwc-website",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -C apps/web dev",
    "assets:sync": "tsx scripts/sync-assets.ts",
    "assets:check": "tsx scripts/check-assets.ts",
    "build": "pnpm assets:check && pnpm build:ci",
    "build:ci": "pnpm -C packages/shared build && pnpm -C apps/web exec prisma generate && pnpm -C apps/web exec next build",
    "start": "pnpm -C apps/web start",
    "test": "pnpm test:ci && pnpm -C apps/web test",
    "lint": "pnpm -C apps/web lint",
    "lint:changed": "node scripts/ci/lint-changed.mjs",
    "test:ci": "node --test tests/ci/*.test.mjs",
    "typecheck": "pnpm -C packages/shared typecheck && pnpm -C apps/web typecheck",
    "db:generate": "pnpm -C apps/web prisma generate",
    "db:migrate": "pnpm -C apps/web prisma migrate dev",
    "db:studio": "pnpm -C apps/web prisma studio"
  },
  "pnpm": {
    "overrides": {
      "@auth/core": "0.41.3"
    }
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "tsx": "^4.21.0"
  },
  "version": "0.1.1.0"
}
